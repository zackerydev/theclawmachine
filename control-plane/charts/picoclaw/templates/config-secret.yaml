{{- if .Values.configFile.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "picoclaw.fullname" . }}-config
  labels:
    {{- include "picoclaw.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{ .Values.configFile.fileName }}: {{ .Values.configFile.content | quote }}
{{- end }}
