apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "picoclaw.fullname" . }}-default-deny
  labels:
    {{- include "picoclaw.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "picoclaw.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "picoclaw.fullname" . }}-allow-dns
  labels:
    {{- include "picoclaw.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "picoclaw.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Egress
  egress:
    - ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
---
{{- if .Values.networkPolicy.ingress }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "picoclaw.fullname" . }}-allow-ingress
  labels:
    {{- include "picoclaw.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "picoclaw.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
  ingress:
    - ports:
        - protocol: TCP
          port: {{ .Values.service.port | default 18790 }}
{{- end }}
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "picoclaw.fullname" . }}-allow-egress
  labels:
    {{- include "picoclaw.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "picoclaw.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Egress
  {{- if .Values.networkPolicy.egress }}
  egress:
    - {}
  {{- else }}
  egress: []
  {{- end }}
