apiVersion: v1
kind: Service
metadata:
  name: {{ include "clawmachine.fullname" . }}
  labels:
    {{- include "clawmachine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "clawmachine.selectorLabels" . | nindent 4 }}
